const user = {
    name: "Иван",
    age: 25,
    city: "Москва"
};


function printUserInfo(user) {
    console.log(`Имя: ${user.name}, Возраст: ${user.age}, Город: ${user.city}`);
}


printUserInfo(user);